%{
    #include <ctype.h>
    #include <stdio.h>
%}

%s MALE WIELKIE

%%

<INITIAL>^[a-z]	    ECHO; BEGIN(MALE);
<MALE>[A-Z]         putchar(tolower(*yytext));
<INITIAL>^[A-Z]	    ECHO; BEGIN(WIELKIE);
<WIELKIE>[a-z]      putchar(toupper(*yytext));
\n                  ECHO; BEGIN(INITIAL);
